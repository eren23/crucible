title: Crucible
description: ML research platform for autonomous experimentation on rental GPUs
theme: jekyll-theme-cayman
# `url` is intentionally unset so Jekyll uses the deployment host's value.
# Set it in your GitHub Pages / deployment config when publishing docs.
baseurl: /crucible
markdown: kramdown
